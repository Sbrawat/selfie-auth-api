# FaceAuth API

A containerized, stateless microservice for facial recognition and biometric authentication.

## Quickstart

1. Ensure Docker is running.
2. Run `docker-compose up --build -d`
3. Navigate to `http://localhost:8000/docs` to view the comprehensive, interactive API documentation (Swagger UI).
